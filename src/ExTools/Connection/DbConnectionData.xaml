<UserControl
    x:Class="ExTools.Connection.DbConnectionData"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:connection="clr-namespace:ExTools.Connection"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=connection:ConnectionEditorViewModel}"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme
                    BaseTheme="Light"
                    PrimaryColor="Indigo"
                    SecondaryColor="Indigo" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush
                x:Key="PrimaryHueMidBrush"
                d:Color="#227447"
                Color="{Binding PrimaryHueMidColor}" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.Resources>
            <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
                <Setter Property="BorderThickness" Value="1" />
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Хост:" />

        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding Host}" />

            <TextBlock
                Grid.Column="1"
                Width="Auto"
                Margin="12,0"
                Text="Порт:" />
            <TextBox
                Grid.Column="2"
                Width="46"
                d:Text="4232"
                materialDesign:TextFieldAssist.CharacterCounterVisibility="Collapsed"
                MaxLength="5"
                Text="{Binding Port}" />
        </Grid>

        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,0"
            Text="База данных:" />
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,8,0,0"
            Text="{Binding Database}" />

        <TextBlock
            Grid.Row="2"
            Margin="0,20,0,0"
            Text="Пользователь:" />
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            Margin="0,20,0,0"
            Text="{Binding User}" />

        <TextBlock
            Grid.Row="3"
            Margin="0,8,0,0"
            Text="Пароль:" />
        <PasswordBox
            x:Name="PasswordBox"
            Grid.Row="3"
            Grid.Column="1"
            Margin="0,8,0,0"
            BorderThickness="1" />
    </Grid>
</UserControl>